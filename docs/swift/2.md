## 打印 Hello Swift

使用 Text 可以在页面上显示文字内容

```swift
import SwiftUI
import SwiftData

struct ContentView: View {

    var body: some View {
      Text("Hello Swift")
    }
}

#Preview {
    ContentView()
}
```
